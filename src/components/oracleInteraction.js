import React from 'react'


function oracleInteraction({inject1, inject2}) {
   // var secDel = inject1 + "security_delay" + inject2
  return (
    <div>
    
    
    </div>
  )
}

export default oracleInteraction